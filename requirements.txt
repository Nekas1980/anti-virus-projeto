# Nenhuma dependência externa necessária
# tkinter está incluído na instalação padrão do Python 3.x
# Bibliotecas utilizadas: hashlib, json, pathlib, os, shutil (todas built-in)

# Dependências opcionais:
# colorama>=0.4.6  # Para output colorido no terminal (ainda não implementado)
